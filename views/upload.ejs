<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Upload</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen font-sans">
    <div class="bg-white shadow-md rounded-lg p-6 w-full max-w-sm">
        <h1 class="text-2xl font-bold text-gray-800 text-center mb-4">Upload a File</h1>
        <form action="/upload" method="POST" enctype="multipart/form-data" class="space-y-4">
            <!-- File Input -->
            <div>
                <label for="image" class="block text-sm font-medium text-gray-700">Choose File:</label>
                <input type="file" id="image" name="image"
                       class="mt-1 w-full border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500">
            </div>
            <!-- Submit Button -->
            <div>
                <input type="submit" value="Upload" 
                       class="w-full bg-blue-500 text-white py-2 px-4 rounded-md font-semibold hover:bg-blue-600 transition">
            </div>
        </form>
    </div>
</body>
</html>
